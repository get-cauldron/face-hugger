---
phase: 05-e2e-ui-automated-testing
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - .github/workflows/test.yml
  - .github/workflows/release.yml
  - src-tauri/Cargo.toml
  - src-tauri/tests/commands_test.rs
  - tests/e2e/smoke/hf-api.spec.ts
autonomous: true
requirements: []

user_setup:
  - service: huggingface
    why: "HF API smoke tests need a dedicated test account token"
    env_vars:
      - name: HF_TEST_TOKEN
        source: "Hugging Face -> Settings -> Access Tokens -> Create token (fine-grained, read-only)"
    dashboard_config:
      - task: "Add HF_TEST_TOKEN as GitHub repository secret"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions -> New repository secret"

must_haves:
  truths:
    - "CI test workflow runs frontend unit tests, Playwright E2E, and Rust tests on all 3 platforms"
    - "Release workflow depends on test workflow passing"
    - "Rust command tests can compile and run with tauri test feature"
    - "HF smoke test runs only when HF_TEST_TOKEN secret is available"
    - "HF smoke test file exists and tests real API connectivity"
  artifacts:
    - path: ".github/workflows/test.yml"
      provides: "CI test workflow with cross-platform matrix"
      contains: "strategy"
    - path: ".github/workflows/release.yml"
      provides: "Release workflow gated on tests"
      contains: "needs.*test"
    - path: "src-tauri/tests/commands_test.rs"
      provides: "Rust command integration tests"
      contains: "mod tests"
    - path: "src-tauri/Cargo.toml"
      provides: "tauri test feature in dev-dependencies"
      contains: "dev-dependencies"
    - path: "tests/e2e/smoke/hf-api.spec.ts"
      provides: "Real HF API smoke tests gated on HF_TEST_TOKEN"
      contains: "HF_TEST_TOKEN"
  key_links:
    - from: ".github/workflows/release.yml"
      to: ".github/workflows/test.yml"
      via: "workflow_run or needs dependency"
      pattern: "needs.*test\\|workflow_run"
    - from: "src-tauri/tests/commands_test.rs"
      to: "src-tauri/Cargo.toml"
      via: "tauri test feature enables tauri::test module"
      pattern: "tauri.*test"
    - from: ".github/workflows/test.yml"
      to: "tests/e2e/smoke/hf-api.spec.ts"
      via: "CI runs smoke tests when HF_TEST_TOKEN is set"
      pattern: "npx playwright test tests/e2e/smoke/"
---

<objective>
Add Rust command tests with tauri::test feature, create HF API smoke tests, create CI test workflow, and gate the release workflow on test passage.

Purpose: This closes the testing loop: Rust backend has command-level tests, real HF API connectivity is validated via smoke tests, and CI ensures no release ships without green tests across all platforms.
Output: CI test workflow, updated release workflow, Rust command tests, HF API smoke spec.
</objective>

<execution_context>
@/Users/zakkeown/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zakkeown/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-e2e-ui-automated-testing/05-RESEARCH.md
@.planning/phases/05-e2e-ui-automated-testing/05-01-SUMMARY.md

@.github/workflows/release.yml
@src-tauri/Cargo.toml
@src-tauri/tests/auth_test.rs
@src-tauri/src/commands/auth.rs
@src-tauri/src/commands/upload.rs
@src-tauri/src/upload/queue.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Rust tauri test feature and command tests</name>
  <files>
    src-tauri/Cargo.toml,
    src-tauri/tests/commands_test.rs
  </files>
  <read_first>
    src-tauri/Cargo.toml,
    src-tauri/tests/auth_test.rs,
    src-tauri/src/commands/auth.rs,
    src-tauri/src/commands/upload.rs,
    src-tauri/src/upload/queue.rs,
    src-tauri/src/lib.rs
  </read_first>
  <action>
    1. Update `src-tauri/Cargo.toml` [dev-dependencies] section:
       Add: `tauri = { version = "2", features = ["test"] }`
       Keep existing: `tempfile = "3"`

    2. Create `src-tauri/tests/commands_test.rs`:
       - This is a Rust integration test file (in tests/ directory)
       - Import `tauri::test::mock_app` (only available with test feature)

       Test "mock_app creates valid app handle":
       - Call mock_app(), get handle, assert handle is valid (non-panic = pass)
       - This validates the tauri test feature is correctly enabled

       Test pure logic functions that don't need AppHandle (per RESEARCH.md Pitfall 6 -- AppHandle<MockRuntime> has trait bound issues):

       Test "compute_xorb_ranges produces correct ranges":
       - Import compute_xorb_ranges from face_hugger_lib (it's a pub helper per STATE.md decisions)
       - Test with a known input size, assert ranges are contiguous and cover the full size

       Test "parse_protocol detects xet vs lfs":
       - Import parse_protocol from face_hugger_lib
       - Test with mock header/response data for both Xet and LFS cases

       Test "format_commit_body produces valid JSON":
       - Import format_commit_body from face_hugger_lib
       - Test with sample file operations, assert output is valid JSON

       Note: Commands with `AppHandle` in their signature (validate_token, start_upload, etc.) cannot be directly tested with MockRuntime due to tauri-specta trait bound issues (documented in RESEARCH.md Pitfall 6). Their pure logic helpers are tested instead. The existing auth_test.rs covers auth integration.

    3. Run `cargo test --manifest-path src-tauri/Cargo.toml` to verify all Rust tests pass.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/face-hugger && cargo test --manifest-path src-tauri/Cargo.toml 2>&1 | tail -15</automated>
  </verify>
  <done>Rust dev-dependencies include tauri test feature, command test file validates mock_app and tests pure helper functions, all Rust tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create HF API smoke test spec</name>
  <files>
    tests/e2e/smoke/hf-api.spec.ts
  </files>
  <read_first>
    tests/fixtures/mocks/ipc.ts,
    tests/fixtures/data/users.ts,
    playwright.config.ts,
    src/components/auth/LoginScreen.tsx
  </read_first>
  <action>
    Create `tests/e2e/smoke/hf-api.spec.ts` with 1-2 real HF API tests gated on process.env.HF_TEST_TOKEN.

    1. At the top of the file, check for HF_TEST_TOKEN:
       ```typescript
       import { test, expect } from '@playwright/test';

       const HF_TOKEN = process.env.HF_TEST_TOKEN;

       test.describe('HF API smoke tests', () => {
         test.skip(!HF_TOKEN, 'HF_TEST_TOKEN not set -- skipping real API tests');
       ```

    2. Test "whoami returns valid user info":
       - Use Playwright's request context (not browser page) to hit the real HF API
       - `const response = await request.get('https://huggingface.co/api/whoami-v2', { headers: { Authorization: `Bearer ${HF_TOKEN}` } })`
       - Assert response.ok() is true
       - Assert response body contains 'name' field (string)
       - Assert response body contains 'type' field equal to 'user' or 'org'

    3. Test "list models returns array":
       - `const response = await request.get('https://huggingface.co/api/models?limit=1', { headers: { Authorization: `Bearer ${HF_TOKEN}` } })`
       - Assert response.ok() is true
       - Assert response body is an array with at least 1 item

    These tests use Playwright's APIRequestContext (test fixture `request`) to make direct HTTP calls -- no browser or Tauri IPC needed. They validate that the HF API is reachable and the test token is valid.

    Do NOT import setupTauriMocks -- these are pure API tests, not browser tests.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/face-hugger && test -f tests/e2e/smoke/hf-api.spec.ts && grep -c "HF_TEST_TOKEN" tests/e2e/smoke/hf-api.spec.ts</automated>
  </verify>
  <done>HF API smoke test file exists with 2 tests gated on HF_TEST_TOKEN, tests whoami and model listing against real HF API</done>
</task>

<task type="auto">
  <name>Task 3: Create CI test workflow and gate release on test passage</name>
  <files>
    .github/workflows/test.yml,
    .github/workflows/release.yml
  </files>
  <read_first>
    .github/workflows/release.yml,
    package.json,
    playwright.config.ts,
    src-tauri/Cargo.toml
  </read_first>
  <action>
    1. Create `.github/workflows/test.yml`:
       ```yaml
       name: Test

       on:
         push:
           branches: [main]
         pull_request:
           branches: [main]
         workflow_call:  # Allow release.yml to call this workflow

       jobs:
         test:
           strategy:
             fail-fast: false
             matrix:
               include:
                 - platform: macos-latest
                 - platform: ubuntu-22.04
                 - platform: windows-latest
           runs-on: ${{ matrix.platform }}

           steps:
             - uses: actions/checkout@v4

             - name: Install Rust stable
               uses: dtolnay/rust-toolchain@stable

             - name: Install dependencies (Ubuntu)
               if: matrix.platform == 'ubuntu-22.04'
               run: |
                 sudo apt-get update
                 sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libayatana-appindicator3-dev

             - uses: actions/setup-node@v4
               with:
                 node-version: lts/*
                 cache: npm

             - name: Install frontend dependencies
               run: npm ci --legacy-peer-deps

             - name: Frontend unit tests
               run: npm test

             - name: Install Playwright browsers
               run: npx playwright install --with-deps chromium

             - name: Playwright E2E tests
               run: npx playwright test

             - name: HF API smoke tests
               if: env.HF_TEST_TOKEN != ''
               env:
                 HF_TEST_TOKEN: ${{ secrets.HF_TEST_TOKEN }}
               run: npx playwright test tests/e2e/smoke/

             - name: Rust unit tests
               run: cargo test --manifest-path src-tauri/Cargo.toml
       ```

    2. Update `.github/workflows/release.yml`:
       - Add a `test` job at the top of jobs that uses the test workflow:
         ```yaml
         jobs:
           test:
             uses: ./.github/workflows/test.yml
             secrets: inherit
         ```
       - Update `build-tauri` job's `needs` from `[create-release]` to `[create-release, test]`
       - This ensures all tests pass before release artifacts are built

    3. Add package.json script (if not already added in Plan 01):
       - Verify "test:e2e": "playwright test" exists in scripts
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/face-hugger && node -e "const yaml = require('js-yaml') || true; const fs = require('fs'); const t = fs.readFileSync('.github/workflows/test.yml','utf8'); const r = fs.readFileSync('.github/workflows/release.yml','utf8'); console.log('test.yml valid:', t.includes('workflow_call') && t.includes('npm test') && t.includes('playwright test') && t.includes('cargo test') && t.includes('HF_TEST_TOKEN')); const needsTest = r.match(/build-tauri:[\\s\\S]*?needs:\\s*\\[([^\\]]+)\\]/); console.log('release needs test:', needsTest && needsTest[1].includes('test')); console.log('release uses test workflow:', r.includes('./.github/workflows/test.yml'));"</automated>
  </verify>
  <done>CI test workflow runs all test layers (Vitest, Playwright, cargo test) on 3 platforms. Release workflow gates artifact builds on test passage. HF smoke tests run only when HF_TEST_TOKEN secret is available. build-tauri needs list includes 'test'.</done>
</task>

</tasks>

<verification>
- `.github/workflows/test.yml` is valid YAML (no syntax errors)
- `.github/workflows/release.yml` has build-tauri depending on test job
- `cargo test --manifest-path src-tauri/Cargo.toml` passes locally
- `tests/e2e/smoke/hf-api.spec.ts` exists and references HF_TEST_TOKEN
- All frontend tests pass: `npm test`
- All Playwright tests pass: `npx playwright test`
</verification>

<success_criteria>
- CI workflow tests all 3 layers on all 3 platforms
- Release workflow blocked by failing tests
- Rust tauri::test feature enabled and command tests pass
- HF smoke test file created with 2 real API tests gated on HF_TEST_TOKEN
- HF smoke tests gated on secret availability in CI
</success_criteria>

<output>
After completion, create `.planning/phases/05-e2e-ui-automated-testing/05-04-SUMMARY.md`
</output>
