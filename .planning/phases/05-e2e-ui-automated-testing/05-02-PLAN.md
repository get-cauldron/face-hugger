---
phase: 05-e2e-ui-automated-testing
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/auth/LoginScreen.test.tsx
  - src/routes/upload/wizard/UploadWizard.test.tsx
  - src/routes/repo-browser/RepoBrowserPage.test.tsx
  - src/routes/datasets/DatasetsPage.test.tsx
  - src/components/UpdateBanner.test.tsx
  - src/queries/useDatasetRows.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "LoginScreen component renders token input, submits, and displays errors"
    - "UploadWizard component manages step transitions correctly"
    - "RepoBrowserPage renders tabs and commit history"
    - "DatasetPreviewPage renders rows and search controls"
    - "UpdateBanner shows when update available and hides otherwise"
  artifacts:
    - path: "src/components/auth/LoginScreen.test.tsx"
      provides: "LoginScreen component tests"
      contains: "describe.*LoginScreen"
    - path: "src/routes/upload/wizard/UploadWizard.test.tsx"
      provides: "UploadWizard step transition tests"
      contains: "describe.*UploadWizard"
    - path: "src/routes/repo-browser/RepoBrowserPage.test.tsx"
      provides: "RepoBrowserPage tab and commit tests"
      contains: "describe.*RepoBrowserPage"
    - path: "src/components/UpdateBanner.test.tsx"
      provides: "UpdateBanner visibility tests"
      contains: "describe.*UpdateBanner"
  key_links:
    - from: "src/components/auth/LoginScreen.test.tsx"
      to: "tests/fixtures/mocks/node.ts"
      via: "MSW server intercepts HF API calls"
      pattern: "import.*msw\\|mock"
    - from: "src/routes/upload/wizard/UploadWizard.test.tsx"
      to: "src/routes/upload/wizard/UploadWizard.tsx"
      via: "renders and tests step transitions"
      pattern: "render.*UploadWizard"
---

<objective>
Fill frontend unit test gaps for untested components and hooks: LoginScreen, UploadWizard, RepoBrowserPage, DatasetsPage, UpdateBanner, and useDatasetRows.

Purpose: The existing 7 frontend tests cover stores, hooks, and utilities. This plan adds component-level tests for the remaining untested UI features.
Output: 6 new test files covering all major untested frontend components.
</objective>

<execution_context>
@/Users/zakkeown/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zakkeown/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-e2e-ui-automated-testing/05-RESEARCH.md
@.planning/phases/05-e2e-ui-automated-testing/05-01-SUMMARY.md

@src/stores/authStore.test.ts
@src/components/shell/AppShell.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for LoginScreen, UploadWizard, and UpdateBanner</name>
  <files>
    src/components/auth/LoginScreen.test.tsx,
    src/routes/upload/wizard/UploadWizard.test.tsx,
    src/components/UpdateBanner.test.tsx
  </files>
  <read_first>
    src/components/auth/LoginScreen.tsx,
    src/routes/upload/wizard/UploadWizard.tsx,
    src/components/UpdateBanner.tsx,
    src/stores/authStore.test.ts,
    src/components/shell/AppShell.test.tsx,
    tests/fixtures/data/users.ts,
    tests/fixtures/data/repos.ts,
    src/bindings.ts,
    src/commands/auth.ts
  </read_first>
  <action>
    All three test files must mock Tauri IPC commands. Follow the pattern from existing tests (AppShell.test.tsx). The app uses `@tauri-apps/api/core` invoke() under the hood, which reads from `window.__TAURI_INTERNALS__`. Mock this in beforeEach using vi.stubGlobal or by setting window.__TAURI_INTERNALS__ directly.

    Also mock `@tauri-apps/plugin-opener` (openUrl), `@tauri-apps/api/event` (listen), `@tauri-apps/plugin-updater` (check) as needed per component imports. Use `vi.mock()` at module level.

    **LoginScreen.test.tsx:**
    - Mock all Tauri modules the component imports (auth commands, openUrl, listen)
    - Test "renders OAuth mode by default": assert "Sign in with Hugging Face" button visible
    - Test "switches to token mode": click "Use access token instead", assert token input appears with placeholder "hf_..."
    - Test "shows error on invalid token": mock validateToken to reject with "Invalid token", fill input with "bad_token", submit form, assert error text "Invalid token" appears
    - Test "shows success on valid token": mock validateToken to resolve with mockUserInfo, fill input with "hf_valid", submit, assert "Welcome, testuser!" appears
    - Test "disables submit when input empty": assert submit button has disabled attribute when input is empty

    **UploadWizard.test.tsx:**
    - Mock Tauri IPC (the wizard steps import commands)
    - Test "renders step 1 (Select Repo) initially": assert "Select Repo" step is active/visible
    - Test "Next button disabled when no repo selected": assert Next/Continue button is disabled
    - Test "advances to step 2 when repo selected": programmatically set selectedRepo state or simulate repo selection, assert step 2 content appears
    - Test "calls onComplete callback": verify the onComplete prop is called after final step submission (mock the upload start command)

    **UpdateBanner.test.tsx:**
    - Mock `@tauri-apps/plugin-updater` check() function
    - Mock `@tauri-apps/plugin-process` relaunch() function
    - Test "renders nothing when no update available": mock check() to return null/undefined, assert component renders nothing (container is empty or null)
    - Test "shows update banner when update available": mock check() to return { version: '0.2.0', body: 'New release' }, assert text containing "0.2.0" or "Update available" appears
    - Test "shows install button in banner": when update available, assert a button with text "Install" or "Update" is present
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/face-hugger && npx vitest run src/components/auth/LoginScreen.test.tsx src/routes/upload/wizard/UploadWizard.test.tsx src/components/UpdateBanner.test.tsx --reporter=dot 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/auth/LoginScreen.test.tsx contains "describe('LoginScreen'"
    - src/components/auth/LoginScreen.test.tsx contains "renders OAuth mode"
    - src/components/auth/LoginScreen.test.tsx contains "shows error on invalid token"
    - src/routes/upload/wizard/UploadWizard.test.tsx contains "describe('UploadWizard'"
    - src/routes/upload/wizard/UploadWizard.test.tsx contains "renders step 1"
    - src/components/UpdateBanner.test.tsx contains "describe('UpdateBanner'"
    - src/components/UpdateBanner.test.tsx contains "no update available"
    - All three test files pass: vitest exits 0
  </acceptance_criteria>
  <done>LoginScreen has 5 tests (OAuth default, token mode switch, error, success, disabled), UploadWizard has 3-4 tests (initial step, disabled next, advance, complete), UpdateBanner has 3 tests (no update, banner visible, install button)</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for RepoBrowserPage, DatasetsPage, and useDatasetRows hook</name>
  <files>
    src/routes/repo-browser/RepoBrowserPage.test.tsx,
    src/routes/datasets/DatasetsPage.test.tsx,
    src/queries/useDatasetRows.test.ts
  </files>
  <read_first>
    src/routes/repo-browser/RepoBrowserPage.tsx,
    src/routes/datasets/DatasetsPage.tsx,
    src/queries/useDatasetRows.ts,
    src/queries/useRepoFiles.test.ts,
    src/queries/useRepos.test.ts,
    tests/fixtures/data/repos.ts,
    tests/fixtures/data/datasets.ts
  </read_first>
  <action>
    Follow the existing test patterns from useRepoFiles.test.ts and useRepos.test.ts for query hook tests (wrap in QueryClientProvider, use waitFor). Follow AppShell.test.tsx pattern for component tests.

    **RepoBrowserPage.test.tsx:**
    - Mock Tauri IPC and React Query data
    - Test "renders file browser tab by default": provide a mock repo via props or context, assert file tree or "Files" tab is active
    - Test "switches to commit history tab": click the "History" or "Commits" tab, assert commit timeline content appears
    - Test "renders delete actions": assert delete file or delete repo buttons/menu items are present
    - Wrap component in QueryClientProvider with appropriate mocked queries

    **DatasetsPage.test.tsx:**
    - Mock Tauri IPC and @huggingface/hub listDatasets
    - Test "renders dataset list": provide mock datasets, assert dataset names appear in the list
    - Test "clicking dataset navigates to preview": simulate click on a dataset, verify onRepoSelect callback is called or preview state changes
    - Wrap in QueryClientProvider

    **useDatasetRows.test.ts:**
    - Follow useRepos.test.ts pattern: wrap hook in renderHook with QueryClientProvider
    - Mock MSW handler for the HF dataset rows API endpoint (or mock @huggingface/hub directly via vi.mock)
    - Test "returns rows for valid dataset": provide repo name, assert hook returns rows array
    - Test "search query filters results": provide searchQuery parameter, assert the query key includes search term
    - Test "handles empty dataset": mock empty response, assert rows is empty array
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/face-hugger && npx vitest run src/routes/repo-browser/RepoBrowserPage.test.tsx src/routes/datasets/DatasetsPage.test.tsx src/queries/useDatasetRows.test.ts --reporter=dot 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/repo-browser/RepoBrowserPage.test.tsx contains "describe('RepoBrowserPage'"
    - src/routes/repo-browser/RepoBrowserPage.test.tsx contains "renders file browser"
    - src/routes/repo-browser/RepoBrowserPage.test.tsx contains "commit history"
    - src/routes/datasets/DatasetsPage.test.tsx contains "describe('DatasetsPage'"
    - src/routes/datasets/DatasetsPage.test.tsx contains "renders dataset list"
    - src/queries/useDatasetRows.test.ts contains "describe.*useDatasetRows"
    - src/queries/useDatasetRows.test.ts contains "returns rows"
    - All three test files pass: vitest exits 0
  </acceptance_criteria>
  <done>RepoBrowserPage has 3 tests (file browser, history tab, delete actions), DatasetsPage has 2 tests (list, click navigation), useDatasetRows has 3 tests (valid rows, search, empty)</done>
</task>

</tasks>

<verification>
- `npm test` passes (all existing + 6 new test files)
- Total frontend test files: 13 (7 existing + 6 new)
</verification>

<success_criteria>
- Every major untested component has at least 2 tests
- All 13 frontend test files pass with `npm test`
- Tests follow existing project patterns (Vitest, RTL, vi.mock for Tauri)
</success_criteria>

<output>
After completion, create `.planning/phases/05-e2e-ui-automated-testing/05-02-SUMMARY.md`
</output>
