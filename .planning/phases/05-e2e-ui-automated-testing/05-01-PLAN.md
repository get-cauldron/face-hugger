---
phase: 05-e2e-ui-automated-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - playwright.config.ts
  - vitest.config.ts
  - tests/fixtures/mocks/handlers.ts
  - tests/fixtures/mocks/node.ts
  - tests/fixtures/mocks/ipc.ts
  - tests/fixtures/data/users.ts
  - tests/fixtures/data/repos.ts
  - tests/fixtures/data/datasets.ts
  - tests/e2e/smoke.spec.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Playwright can launch Chromium, open the Vite dev server at localhost:1420, and pass a trivial assertion"
    - "MSW node server intercepts HTTP requests in Vitest tests"
    - "Tauri IPC mock map provides typed command->response stubs for all app commands"
    - "Test fixture data matches the app's actual TypeScript types"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright configuration targeting localhost:1420"
      contains: "baseURL.*1420"
    - path: "tests/fixtures/mocks/handlers.ts"
      provides: "MSW v2 HTTP handlers for HF API"
      exports: ["handlers"]
    - path: "tests/fixtures/mocks/node.ts"
      provides: "MSW node server for Vitest"
      exports: ["server"]
    - path: "tests/fixtures/mocks/ipc.ts"
      provides: "Tauri IPC mock command map"
      exports: ["ipcHandlers", "setupTauriMocks"]
    - path: "tests/fixtures/data/users.ts"
      provides: "Mock UserInfo fixtures"
      exports: ["mockUserInfo"]
    - path: "tests/fixtures/data/repos.ts"
      provides: "Mock RepoItem fixtures"
      exports: ["mockRepos", "mockModelRepo", "mockDatasetRepo"]
    - path: "tests/e2e/smoke.spec.ts"
      provides: "Validation that Playwright can run against Vite dev server"
      contains: "test.*page.*title"
  key_links:
    - from: "tests/fixtures/mocks/node.ts"
      to: "tests/fixtures/mocks/handlers.ts"
      via: "imports handlers array"
      pattern: "import.*handlers.*from.*handlers"
    - from: "vitest.config.ts"
      to: "tests/fixtures/mocks/node.ts"
      via: "setupFiles references MSW setup"
      pattern: "setupFiles"
---

<objective>
Install Playwright and MSW, create shared test fixtures, IPC mock map, and Playwright config. Validate the entire test infrastructure works with a trivial smoke spec.

Purpose: All subsequent plans (unit test gaps, E2E specs, CI) depend on this infrastructure being in place and verified.
Output: Working Playwright + MSW setup, shared fixtures, one passing E2E smoke test.
</objective>

<execution_context>
@/Users/zakkeown/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zakkeown/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-e2e-ui-automated-testing/05-RESEARCH.md

@package.json
@vitest.config.ts
@src/lib/types.ts
@src/queries/useRepos.ts
@src/stores/authStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, create Playwright config, MSW handlers, and IPC mock map</name>
  <files>
    package.json,
    playwright.config.ts,
    vitest.config.ts,
    tests/fixtures/mocks/handlers.ts,
    tests/fixtures/mocks/node.ts,
    tests/fixtures/mocks/setup.ts,
    tests/fixtures/mocks/ipc.ts,
    tests/fixtures/data/users.ts,
    tests/fixtures/data/repos.ts,
    tests/fixtures/data/datasets.ts
  </files>
  <read_first>
    package.json,
    vitest.config.ts,
    src/lib/types.ts,
    src/queries/useRepos.ts,
    src/stores/authStore.ts,
    src/bindings.ts,
    .planning/phases/05-e2e-ui-automated-testing/05-RESEARCH.md
  </read_first>
  <action>
    1. Install Playwright and MSW:
       ```
       npm install -D @playwright/test msw --legacy-peer-deps
       npx playwright install chromium
       ```

    2. Create `playwright.config.ts` at project root:
       - testDir: './tests/e2e'
       - timeout: 30_000
       - use.baseURL: 'http://localhost:1420'
       - use.screenshot: 'only-on-failure'
       - use.trace: 'on-first-retry'
       - webServer.command: 'npm run dev'
       - webServer.url: 'http://localhost:1420'
       - webServer.reuseExistingServer: !process.env.CI
       - webServer.timeout: 30_000
       - projects: [{ name: 'chromium', use: { browserName: 'chromium' } }]
       - reporter: process.env.CI ? 'github' : 'list'

    3. Create `tests/fixtures/mocks/handlers.ts` with MSW v2 handlers:
       - Use `http` and `HttpResponse` from 'msw' (NOT `rest` — that is MSW v1)
       - `http.get('https://huggingface.co/api/whoami-v2', ...)` returns mock UserInfo JSON
       - `http.get('https://huggingface.co/api/models', ...)` returns array of mock model objects
       - `http.get('https://huggingface.co/api/datasets', ...)` returns array of mock dataset objects
       - Export `handlers` array

    4. Create `tests/fixtures/mocks/node.ts`:
       - Import `setupServer` from 'msw/node'
       - Import `handlers` from './handlers'
       - Export `server = setupServer(...handlers)`

    5. Create `tests/fixtures/mocks/setup.ts` (Vitest setup file):
       - Import `beforeAll, afterEach, afterAll` from 'vitest'
       - Import `server` from './node'
       - beforeAll(() => server.listen({ onUnhandledRequest: 'warn' }))
       - afterEach(() => server.resetHandlers())
       - afterAll(() => server.close())

    6. Update `vitest.config.ts`:
       - Add `'./tests/fixtures/mocks/setup.ts'` to the setupFiles array
       - Keep existing config (jsdom environment, @ alias)

    7. Create `tests/fixtures/mocks/ipc.ts`:
       - Export `ipcHandlers`: a Record<string, (args?: any) => any> mapping Tauri command names to mock return values
       - Include all commands the app calls on startup: check_existing_token (returns null), get_stored_token (returns null), validate_token (returns mockUserInfo), logout (returns null), start_upload (returns 'queued'), cancel_upload (returns null), pause_upload (returns null), resume_upload (returns null), get_queue_state (returns []), set_max_concurrent (returns null), start_progress_emitter (returns null), oauth_start (returns 'https://huggingface.co/oauth/authorize'), oauth_exchange_code (returns mockUserInfo), oauth_cancel (returns null)
       - Export `setupTauriMocks(page)` async function for Playwright that calls page.addInitScript with the __TAURI_INTERNALS__ stub (invoke, metadata.currentWindow.label='main', convertFileSrc identity function)
       - The addInitScript must stringify and pass the handler map as a parameter since it runs in browser context

    8. Create `tests/fixtures/data/users.ts`:
       - Export `mockUserInfo` matching UserInfo type: { name: 'testuser', fullname: 'Test User', avatar_url: 'https://example.com/avatar.png', email: 'test@example.com', type: 'user' }

    9. Create `tests/fixtures/data/repos.ts`:
       - Import RepoItem type from '@/queries/useRepos' (or inline the shape)
       - Export `mockModelRepo`: { id: 'testuser/test-model', name: 'testuser/test-model', type: 'model' as const, private: false, downloads: 100, likes: 10, lastModified: '2026-01-01T00:00:00Z', tags: ['pytorch'] }
       - Export `mockDatasetRepo`: { id: 'testuser/test-dataset', name: 'testuser/test-dataset', type: 'dataset' as const, private: false, downloads: 50, likes: 5, lastModified: '2026-01-01T00:00:00Z', tags: ['csv'] }
       - Export `mockRepos = [mockModelRepo, mockDatasetRepo]`

    10. Create `tests/fixtures/data/datasets.ts`:
        - Export `mockDatasetRows`: array of 5 objects with columns { text: string, label: number, score: number }
        - Export `mockColumnStats`: object with column names as keys and { type: 'string'|'int'|'float', count: number, nullCount: number } as values

    11. Add npm scripts to package.json:
        - "test:e2e": "playwright test"
        - "test:e2e:ui": "playwright test --ui"
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/face-hugger && npx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "@playwright/test" in devDependencies
    - package.json contains "msw" in devDependencies
    - package.json contains "test:e2e" in scripts
    - playwright.config.ts contains "baseURL: 'http://localhost:1420'"
    - playwright.config.ts contains "testDir: './tests/e2e'"
    - vitest.config.ts setupFiles array contains './tests/fixtures/mocks/setup.ts'
    - tests/fixtures/mocks/handlers.ts contains "import { http, HttpResponse } from 'msw'"
    - tests/fixtures/mocks/handlers.ts contains "export const handlers"
    - tests/fixtures/mocks/node.ts contains "import { setupServer } from 'msw/node'"
    - tests/fixtures/mocks/ipc.ts contains "export const ipcHandlers"
    - tests/fixtures/mocks/ipc.ts contains "export async function setupTauriMocks"
    - tests/fixtures/mocks/ipc.ts contains "__TAURI_INTERNALS__"
    - tests/fixtures/data/users.ts contains "export const mockUserInfo"
    - tests/fixtures/data/repos.ts contains "export const mockModelRepo"
    - tests/fixtures/data/repos.ts contains "export const mockDatasetRepo"
    - tests/fixtures/data/datasets.ts contains "export const mockDatasetRows"
    - Existing Vitest tests still pass: `npm test` exits 0
  </acceptance_criteria>
  <done>Playwright installed with Chromium, MSW v2 configured for Vitest, IPC mock map covers all app commands, fixture data matches app types, existing tests still pass</done>
</task>

<task type="auto">
  <name>Task 2: Create smoke E2E spec to validate Playwright runs against Vite dev server</name>
  <files>tests/e2e/smoke.spec.ts</files>
  <read_first>
    playwright.config.ts,
    tests/fixtures/mocks/ipc.ts,
    src/components/auth/LoginScreen.tsx,
    src/components/shell/AppShell.tsx
  </read_first>
  <action>
    Create `tests/e2e/smoke.spec.ts`:

    1. Import `test, expect` from '@playwright/test'
    2. Import `setupTauriMocks` from the ipc fixtures (use relative path '../fixtures/mocks/ipc' since Playwright does not use the @ alias)

    3. test.beforeEach: call setupTauriMocks(page) then page.goto('/')

    4. Test "app loads and shows login screen":
       - The app starts unauthenticated (check_existing_token returns null)
       - Assert page contains text "Sign in to Face Hugger"
       - Assert the "Sign in with Hugging Face" button is visible

    5. Test "page has correct title":
       - Assert page title contains "Face Hugger" or is the Vite default — just verify page.title() resolves without error

    Note: The setupTauriMocks function injects __TAURI_INTERNALS__ via addInitScript BEFORE page.goto, which is critical for avoiding "invoke not defined" errors on app startup.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/face-hugger && npx playwright test tests/e2e/smoke.spec.ts --reporter=list 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/smoke.spec.ts contains "test.*app loads"
    - tests/e2e/smoke.spec.ts contains "setupTauriMocks"
    - tests/e2e/smoke.spec.ts contains "Sign in to Face Hugger"
    - `npx playwright test tests/e2e/smoke.spec.ts` exits 0
  </acceptance_criteria>
  <done>Playwright smoke test passes against Vite dev server with Tauri IPC mocked, proving the test infrastructure works end-to-end</done>
</task>

</tasks>

<verification>
- `npm test` passes (existing Vitest tests + MSW setup integration)
- `npx playwright test tests/e2e/smoke.spec.ts` passes (Playwright connects to Vite dev server, IPC mocked)
- All fixture files exist and export expected symbols
</verification>

<success_criteria>
- Playwright installed with Chromium browser
- MSW v2 integrated into Vitest via setupFiles
- IPC mock map covers all Tauri commands the app calls
- One Playwright smoke test passes end-to-end
- Existing 7 Vitest tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-e2e-ui-automated-testing/05-01-SUMMARY.md`
</output>
