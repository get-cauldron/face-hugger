---
phase: 04-secondary-features-distribution
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - .github/workflows/release.yml
  - src-tauri/tauri.conf.json
  - src/components/UpdateBanner.tsx
  - src/components/shell/AppShell.tsx
autonomous: false
requirements: [AUTH-03, UPLD-09, DATA-01, DATA-02, DATA-03]
user_setup:
  - service: github-actions
    why: "CI/CD pipeline for building and releasing signed artifacts"
    env_vars:
      - name: TAURI_SIGNING_PRIVATE_KEY
        source: "Generated via `npm run tauri signer generate` — store the private key content as a GitHub Actions secret"
      - name: TAURI_SIGNING_PRIVATE_KEY_PASSWORD
        source: "Password set during key generation (can be empty) — store as GitHub Actions secret"
    dashboard_config:
      - task: "Generate Tauri updater signing keypair"
        location: "Run `npm run tauri signer generate -- -w ~/.tauri/face-hugger.key` locally"
      - task: "Add TAURI_SIGNING_PRIVATE_KEY secret to GitHub repo"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions -> New repository secret"
  - service: apple-developer
    why: "macOS code signing and notarization"
    env_vars:
      - name: APPLE_CERTIFICATE
        source: "Base64-encoded .p12 certificate from Apple Developer account"
      - name: APPLE_CERTIFICATE_PASSWORD
        source: "Password for the .p12 certificate"
      - name: APPLE_ID
        source: "Apple ID email used for notarization"
      - name: APPLE_PASSWORD
        source: "App-specific password from appleid.apple.com"
      - name: APPLE_TEAM_ID
        source: "Team ID from Apple Developer account membership page"
    dashboard_config:
      - task: "Create Developer ID Application certificate"
        location: "Apple Developer -> Certificates, Identifiers & Profiles -> Create Certificate -> Developer ID Application"
      - task: "Generate app-specific password for notarization"
        location: "appleid.apple.com -> Security -> App-Specific Passwords -> Generate"
  - service: hf-oauth
    why: "OAuth app registration for HF login"
    env_vars: []
    dashboard_config:
      - task: "Register OAuth application on Hugging Face"
        location: "https://huggingface.co/settings/applications/new — set redirect URI to http://localhost, type: public (no secret), scopes: openid profile write-repos"
      - task: "Copy client_id into src-tauri/src/commands/auth.rs HF_OAUTH_CLIENT_ID constant"
        location: "After registration, HF shows the client_id"

must_haves:
  truths:
    - "GitHub Actions workflow builds signed artifacts for macOS, Windows, and Linux on tag push"
    - "Auto-update check runs on app launch and shows banner when new version available"
    - "Update banner shows version number with 'Update now' and 'Dismiss' buttons"
    - "All three platforms produce distributable artifacts (dmg, msi/exe, AppImage/deb)"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "GitHub Actions CI workflow for building and releasing Tauri app"
      contains: "tauri-apps/tauri-action"
    - path: "src/components/UpdateBanner.tsx"
      provides: "Sticky banner component for update notifications"
      contains: "Update now"
  key_links:
    - from: "src/components/UpdateBanner.tsx"
      to: "@tauri-apps/plugin-updater"
      via: "check() on mount, download+relaunch on click"
      pattern: "check"
    - from: ".github/workflows/release.yml"
      to: "src-tauri/tauri.conf.json"
      via: "Reads bundle config, updater endpoints from tauri.conf.json"
      pattern: "tauri-action"
---

<objective>
Create GitHub Actions CI workflow for cross-platform release builds and implement auto-update with UpdateBanner component.

Purpose: Distribution + auto-update -- users get signed, notarized artifacts for all platforms with built-in update checking.
Output: CI workflow file, updater configuration in tauri.conf.json, and UpdateBanner component in AppShell.
</objective>

<execution_context>
@/Users/zakkeown/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zakkeown/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-secondary-features-distribution/04-CONTEXT.md
@.planning/phases/04-secondary-features-distribution/04-RESEARCH.md
@.planning/phases/04-secondary-features-distribution/04-UI-SPEC.md

<interfaces>
From src-tauri/tauri.conf.json:
```json
{
  "productName": "face-hugger",
  "version": "0.1.0",
  "identifier": "com.face-hugger.app",
  "bundle": {
    "active": true,
    "targets": "all",
    "icon": ["icons/32x32.png", "icons/128x128.png", "icons/128x128@2x.png", "icons/icon.icns", "icons/icon.ico"]
  }
}
```

From src/components/shell/AppShell.tsx:
```typescript
// AppShell renders: TitleBar, Sidebar, main content area
// UpdateBanner goes at top of <main> before ContentArea
<main className="flex-1 overflow-auto p-6 bg-background relative">
  <ContentArea ... />
</main>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions release workflow and tauri.conf.json updater config</name>
  <files>
    .github/workflows/release.yml,
    src-tauri/tauri.conf.json
  </files>
  <read_first>
    src-tauri/tauri.conf.json,
    package.json,
    src-tauri/Cargo.toml
  </read_first>
  <action>
**1. Create .github/workflows/ directory if it doesn't exist.**

**2. Create .github/workflows/release.yml:**

```yaml
name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  create-release:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    outputs:
      release_id: ${{ steps.create-release.outputs.result }}
    steps:
      - uses: actions/checkout@v4
      - name: Create release
        id: create-release
        uses: actions/github-script@v7
        with:
          script: |
            const { data } = await github.rest.repos.createRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              tag_name: `${context.ref.replace('refs/tags/', '')}`,
              name: `Face Hugger ${context.ref.replace('refs/tags/', '')}`,
              body: 'See the assets to download and install this version.',
              draft: true,
              prerelease: false,
            });
            return data.id;

  build-tauri:
    needs: create-release
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            args: '--target aarch64-apple-darwin'
          - platform: macos-latest
            args: '--target x86_64-apple-darwin'
          - platform: ubuntu-22.04
            args: ''
          - platform: windows-latest
            args: ''
    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Install dependencies (Ubuntu)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libayatana-appindicator3-dev

      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: 'npm'

      - name: Install frontend dependencies
        run: npm ci --legacy-peer-deps

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          # macOS signing (only on macos runner)
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        with:
          releaseId: ${{ needs.create-release.outputs.release_id }}
          args: ${{ matrix.args }}

  publish-release:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    needs: [create-release, build-tauri]
    steps:
      - name: Publish release
        uses: actions/github-script@v7
        env:
          release_id: ${{ needs.create-release.outputs.release_id }}
        with:
          script: |
            github.rest.repos.updateRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              release_id: process.env.release_id,
              draft: false,
              prerelease: false,
            });
```

**3. Update tauri.conf.json with updater and bundle config:**

Add `createUpdaterArtifacts` to bundle:
```json
"bundle": {
    "active": true,
    "targets": "all",
    "createUpdaterArtifacts": "v2Compatible",
    "icon": [...]
}
```

Add `plugins` section with updater config. The pubkey will be a placeholder until the user generates the key pair:
```json
"plugins": {
    "updater": {
        "pubkey": "PLACEHOLDER_GENERATE_WITH_npm_run_tauri_signer_generate",
        "endpoints": [
            "https://github.com/OWNER/face-hugger/releases/latest/download/latest.json"
        ]
    }
}
```

Note: The OWNER in the endpoint URL should match the actual GitHub repository owner. The executor should check the git remote URL (`git remote get-url origin`) to determine the correct owner/repo path.

Add Linux-specific bundle config for libayatana-appindicator dependency:
```json
"bundle": {
    ...
    "linux": {
        "deb": {
            "depends": ["libayatana-appindicator3-1"]
        }
    }
}
```

**4. Update CSP if not already done in Plan 01:**

Ensure the CSP `connect-src` includes the GitHub releases endpoint for update checks. Actually, the updater plugin uses Rust-side HTTP (reqwest), not frontend fetch, so no CSP change needed for updates.
  </action>
  <verify>
    <automated>cat /Users/zakkeown/Code/face-hugger/.github/workflows/release.yml | head -5 && echo "---" && grep -c "tauri-action" /Users/zakkeown/Code/face-hugger/.github/workflows/release.yml && echo "---" && grep "createUpdaterArtifacts" /Users/zakkeown/Code/face-hugger/src-tauri/tauri.conf.json</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/release.yml exists
    - .github/workflows/release.yml contains `tauri-apps/tauri-action`
    - .github/workflows/release.yml contains `macos-latest`
    - .github/workflows/release.yml contains `ubuntu-22.04`
    - .github/workflows/release.yml contains `windows-latest`
    - .github/workflows/release.yml contains `TAURI_SIGNING_PRIVATE_KEY`
    - .github/workflows/release.yml contains `APPLE_CERTIFICATE`
    - .github/workflows/release.yml contains `libayatana-appindicator3-dev`
    - .github/workflows/release.yml contains `--legacy-peer-deps`
    - src-tauri/tauri.conf.json contains `createUpdaterArtifacts`
    - src-tauri/tauri.conf.json contains `updater`
    - src-tauri/tauri.conf.json contains `pubkey`
    - src-tauri/tauri.conf.json contains `endpoints`
    - src-tauri/tauri.conf.json contains `libayatana-appindicator3-1`
  </acceptance_criteria>
  <done>GitHub Actions release workflow builds signed artifacts for macOS (arm64 + x86_64), Windows, and Linux on tag push. Creates draft release, uploads artifacts, publishes when all builds succeed. tauri.conf.json configured with updater endpoints and bundle settings including Linux deb dependency.</done>
</task>

<task type="auto">
  <name>Task 2: UpdateBanner component and integration into AppShell</name>
  <files>
    src/components/UpdateBanner.tsx,
    src/components/shell/AppShell.tsx
  </files>
  <read_first>
    src/components/shell/AppShell.tsx,
    src-tauri/tauri.conf.json
  </read_first>
  <action>
**1. Create src/components/UpdateBanner.tsx:**

```typescript
import { useState, useEffect } from 'react';
import { ArrowUpCircle } from 'lucide-react';
import { check, Update } from '@tauri-apps/plugin-updater';
import { relaunch } from '@tauri-apps/plugin-process';

export default function UpdateBanner() {
  const [update, setUpdate] = useState<Update | null>(null);
  const [dismissed, setDismissed] = useState(false);
  const [downloading, setDownloading] = useState(false);

  useEffect(() => {
    check()
      .then((u) => { if (u) setUpdate(u); })
      .catch(() => {}); // Non-fatal — never block app launch
  }, []);

  if (!update || dismissed) return null;

  const handleUpdate = async () => {
    setDownloading(true);
    try {
      await update.downloadAndInstall();
      await relaunch();
    } catch {
      setDownloading(false);
    }
  };

  return (
    <div className="flex items-center justify-between bg-card border-b border-border px-4 h-12 flex-shrink-0">
      <div className="flex items-center gap-2">
        <ArrowUpCircle className="w-4 h-4 text-primary" />
        <span className="text-sm text-foreground">
          Version {update.version} is available
        </span>
      </div>
      <div className="flex items-center gap-3">
        <button
          onClick={handleUpdate}
          disabled={downloading}
          className="text-sm text-primary hover:underline cursor-pointer disabled:opacity-50"
        >
          {downloading ? 'Downloading...' : 'Update now'}
        </button>
        <button
          onClick={() => setDismissed(true)}
          className="text-sm text-muted-foreground hover:text-foreground cursor-pointer"
        >
          Dismiss
        </button>
      </div>
    </div>
  );
}
```

Note: `@tauri-apps/plugin-process` may need to be installed for `relaunch()`. Check if it's already a dependency. If not, install:
```bash
npm install @tauri-apps/plugin-process --legacy-peer-deps
```
And add the plugin in Rust if needed: `tauri-plugin-process` in Cargo.toml and `.plugin(tauri_plugin_process::init())` in lib.rs.

Actually, Tauri v2 may include process relaunch as a core feature. Check the `@tauri-apps/plugin-updater` docs — the `Update` object may have a `installAndRelaunch()` method that handles this. Use whatever the updater plugin provides. If `relaunch` requires a separate plugin, install it.

**2. Integrate UpdateBanner into AppShell.tsx:**

Import at top:
```typescript
import UpdateBanner from '../UpdateBanner';
```

Add inside the main content area, before `ContentArea`, after `TitleBar`:

Change the `<main>` section to:
```tsx
<main className="flex-1 flex flex-col overflow-hidden">
  <UpdateBanner />
  <div className="flex-1 overflow-auto p-6 bg-background relative">
    <ContentArea ... />
    {/* Drag-over overlay */}
    {isDragOver && ( ... )}
  </div>
</main>
```

The `flex flex-col` on main ensures UpdateBanner takes its natural height (48px when visible, 0 when absent) and the content area fills the rest. The `overflow-hidden` on main prevents double scrollbars; `overflow-auto` stays on the inner div.

**3. Verify build passes:**

Run `npm run build` to confirm no import errors or type issues.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/face-hugger && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/UpdateBanner.tsx contains `Version`
    - src/components/UpdateBanner.tsx contains `is available`
    - src/components/UpdateBanner.tsx contains `Update now`
    - src/components/UpdateBanner.tsx contains `Dismiss`
    - src/components/UpdateBanner.tsx contains `Downloading...`
    - src/components/UpdateBanner.tsx contains `ArrowUpCircle`
    - src/components/UpdateBanner.tsx contains `check`
    - src/components/UpdateBanner.tsx contains `downloadAndInstall` or `installAndRelaunch`
    - src/components/shell/AppShell.tsx contains `UpdateBanner`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>UpdateBanner checks for updates on mount, shows version and "Update now" / "Dismiss" buttons. Integrated at top of AppShell main content area. Dismiss hides for session. Download triggers install and relaunch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all Phase 4 features</name>
  <action>Human verifies OAuth login, dataset preview, tray, and CI workflow.</action>
  <what-built>Complete Phase 4: OAuth login, dataset preview with search/filter/stats, system tray with close-to-tray and notifications, CI release workflow, and auto-update banner.</what-built>
  <how-to-verify>
    1. **OAuth Login:** Launch app. Verify login screen shows "Sign in with Hugging Face" as primary button with "Use access token instead" below.
    2. **Token Fallback:** Click "Use access token instead" — verify token paste form appears with "Back to OAuth sign-in" link.
    3. **Dataset Preview:** Navigate to a dataset repo. Verify "Preview" tab appears (not visible for model repos). Click Preview — verify table loads with rows, column type badges, and pagination.
    4. **Search:** Type in search bar — verify rows filter and matches are highlighted.
    5. **Column Stats:** Click a column header — verify side panel slides in with statistics and histogram chart.
    6. **Tray (visual):** Check that system tray icon appears when app is running.
    7. **Update Banner:** If no update available, banner should not appear (correct behavior for dev build).
    8. **CI Workflow:** Verify `.github/workflows/release.yml` exists and looks correct.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes
- `cargo check` passes
- `.github/workflows/release.yml` contains matrix builds for all three platforms
- `tauri.conf.json` contains updater config with endpoints
- UpdateBanner component renders conditionally in AppShell
- All Phase 4 features visually verified by user
</verification>

<success_criteria>
- Distribution pipeline ready (CI workflow, signing config, updater endpoints)
- Auto-update banner checks on launch and offers update + dismiss
- All Phase 4 requirement IDs covered: AUTH-03, UPLD-09, DATA-01, DATA-02, DATA-03
- Phase 4 human verification checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/04-secondary-features-distribution/04-04-SUMMARY.md`
</output>
