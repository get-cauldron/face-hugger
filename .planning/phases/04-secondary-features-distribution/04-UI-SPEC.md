---
phase: 4
slug: secondary-features-distribution
status: draft
shadcn_initialized: true
preset: base-nova
created: 2026-03-19
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn |
| Preset | base-nova |
| Component library | @base-ui/react (via base-nova style) |
| Icon library | lucide-react |
| Font | -apple-system, BlinkMacSystemFont, "Segoe UI", system-ui, sans-serif |

Source: `components.json` + `src/app.css`

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, type badge padding (`px-1 py-0`) |
| sm | 8px | Compact element spacing, tab bar gap |
| md | 16px | Default element spacing, table cell padding |
| lg | 24px | Section padding, card interior |
| xl | 32px | Layout gaps between major regions |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions:
- Touch/click targets minimum 44px height — applied to OAuth primary button, tray menu items
- Dataset table rows: 40px row height for data density (multiple of 4, between md and xl)
- Column stat side panel width: 320px fixed (40 * 8 grid unit)

Type badge padding: `px-1 py-0` (4px horizontal, 0px vertical). The 2px vertical padding (`py-0.5`) used previously is not on the 4-grid and is dropped in favor of line-height providing sufficient visual breathing room at 12px/1.4 label size.

Source: 8-point scale established in Phase 1/3; exceptions are defaults for this phase.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 (regular) | 1.5 |
| Label | 12px | 400 (regular) | 1.4 |
| Heading | 20px | 600 (semibold) | 1.2 |
| Display | 28px | 600 (semibold) | 1.1 |

Notes:
- Body (14px/400) used for: dataset table cell values, tray menu descriptions, OAuth waiting message
- Label (12px/400) used for: column type badges, pagination counts, table column headers, update banner version string
- Heading (20px/600) used for: dataset preview section title, OAuth screen heading ("Sign in to Face Hugger")
- Display (28px/600) not introduced as new surface in Phase 4 — inherited from existing login screen h1

Weight rationale: Collapsed from 4 weights (400/500/600/700) to 2. Weight 500 (medium) merged into 400 — the visual distinction between 400 and 500 at 12px label size is negligible. Weight 700 (bold) merged into 600 — Display text at 28px reads clearly at semibold without requiring bold.

Source: Inferred from existing `LoginScreen.tsx` (`text-2xl font-bold`), `RepoBrowserPage.tsx` (`text-lg font-semibold`), and `text-sm` labels throughout Phase 3 components.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `oklch(0.13 0.01 240)` — near-black blue-gray | Main background, dataset table background, OAuth waiting screen background |
| Secondary (30%) | `oklch(0.16 0.01 240)` — dark card / `oklch(0.11 0.01 240)` — sidebar | Dataset preview panel, column stat side panel, update banner background, OAuth card |
| Accent (10%) | `oklch(0.72 0.19 45)` — warm orange | Primary OAuth "Sign in with Hugging Face" button, active tab underline, search highlight text color, update banner CTA |
| Destructive | `oklch(0.60 0.25 25)` — muted red | No destructive actions introduced in Phase 4 — inherited for potential future errors |

Source: `src/app.css` CSS custom properties.

Accent reserved for:
1. Primary CTA button — "Sign in with Hugging Face" (OAuth login)
2. Active tab underline indicator — "Preview" tab active state in repo browser
3. Search hit highlight — `<mark>` or highlighted span background around matched text in dataset table cells
4. Update banner "Update now" link/button
5. Pagination "current page" indicator in dataset table

---

## Component Inventory

New shadcn components required for Phase 4 (not yet in `src/components/ui/`):

| Component | Usage | Install Command |
|-----------|-------|-----------------|
| `table` | Dataset preview — rows, headers, column badges | `npx shadcn add table` |
| `badge` | Column type labels (string, int, float, bool, etc.) | `npx shadcn add badge` |
| `select` | Split/config selector in dataset preview, per-column filter dropdown | `npx shadcn add select` |
| `input` | Global search bar in dataset preview | `npx shadcn add input` |
| `separator` | Visual dividers in column stat panel | `npx shadcn add separator` |
| `skeleton` | Loading shimmer for dataset table rows | `npx shadcn add skeleton` |

Already installed (do not reinstall): `alert-dialog`, `button`, `dialog`, `progress`, `sheet`, `tooltip`

Third-party library (not via shadcn registry):
- `recharts` — column statistics histogram/bar chart in `ColumnStatPanel`

---

## Surface Inventory

Every new UI surface introduced in Phase 4:

### 1. OAuth Login Screen (modified LoginScreen.tsx)

**Layout:** Full-screen centered card (existing card pattern, `max-w-md`, `bg-card rounded-2xl`)

**States and their visuals:**

| State | What user sees |
|-------|---------------|
| Default | Face Hugger icon + "Sign in to Face Hugger" heading + primary "Sign in with Hugging Face" OAuth button + small "Use access token instead" text link below |
| Waiting for browser | Spinner (Lucide `Loader2` animated) + "Waiting for browser login..." body text + secondary "Cancel Sign-in" button |
| OAuth error | Red inline error under the card heading + "Try again" link that resets to Default state |
| Token fallback | Existing token paste form slides in below — same layout as current LoginScreen |

**OAuth button:** Full-width, `bg-primary text-primary-foreground`, 44px min height, rounded-lg, Hugging Face logo icon on left (use `img` tag with HF favicon or SVG inline).

**"Use access token instead" link:** 14px, `text-muted-foreground`, centered below OAuth button, `hover:text-foreground`.

**Waiting spinner:** `Loader2` from lucide-react, `animate-spin`, 24px, `text-primary`, centered above waiting text.

### 2. Dataset Preview Tab (new tab in RepoBrowserPage.tsx)

**Tab label:** "Preview" — only rendered when `repoType === 'dataset'` and `/is-valid` returns `viewer: true`. Hidden entirely for model repos.

**Layout:** Full-height flex column within existing tab content area.

**Primary visual anchor:** The scrollable table body is the focal point of this surface. All toolbar controls (search bar, split selector, filter dropdowns) are subordinate and positioned in a single compact row above the table. The table occupies the maximum available height; the toolbar row and pagination footer are fixed-height so they do not compete for attention.

**Sub-components:**

- **Split selector** (`Select` from shadcn): appears if dataset has multiple configs/splits. Label: "Split:". Positioned above table, right-aligned with search bar.
- **Search bar** (`Input` from shadcn): left side of toolbar row above table. Placeholder: "Search rows...". Width: 280px. Lucide `Search` icon inside input left side.
- **Filter dropdowns**: per-column, revealed via column header click on string/class columns. Uses shadcn `Select` or custom dropdown — Excel-style with value list.
- **Table** (`Table` from shadcn): fixed header, scrollable body. Column width: equal distribution unless values are truncated (max 240px per column).
- **Column header badge**: `Badge` component, 10px text, muted background (`bg-secondary`), colored text per type. Badge padding: `px-1 py-0`. Colors: string=blue-ish `text-blue-400`, int=amber `text-amber-400`, float=amber `text-amber-400`, bool=green `text-green-400`, other=`text-muted-foreground`.
- **Row count footer**: "Rows {start}–{end} of {total}" + pagination prev/next buttons. 14px, `text-muted-foreground`.
- **Search highlight**: matched substring wrapped in `<mark className="bg-primary/20 text-primary rounded-sm px-0.5">`.

**Column stat side panel**: slides in from right edge of preview area (not full-screen sheet). Width 320px. `bg-card border-l border-border`. Shows: column name heading, type badge, null count, min/max, mean, unique count, and a `recharts BarChart` histogram (if numeric/class). Close via X button top-right with `aria-label="Close statistics panel"`.

**Loading state:** `Skeleton` rows — 8 rows × full table width, 40px height each, `animate-pulse`.

**Preview unavailable state:** Centered in tab content: "Preview unavailable" heading (20px/semibold), body "This dataset hasn't been processed for preview yet." — no retry button (HF processes asynchronously; instruct user to check back).

### 3. Update Banner (new sticky component in AppShell.tsx)

**Position:** Top of `<main>` content area, above page content, below TitleBar. Full-width of main content area.

**Visual:** `bg-card border-b border-border`, 48px height, single row. Left: Lucide `ArrowUpCircle` icon (16px, `text-primary`) + "Version {X.Y.Z} is available" (14px, `text-foreground`). Right: "Update now" button (small, accent-colored, `variant="link"` or `variant="ghost"`) + "Dismiss" text button (`text-muted-foreground`).

**Behavior:** Dismiss hides for this session (not persisted — banner returns on next launch if update still pending). "Update now" triggers `relaunch()` after download.

**States:** Banner is absent until `check()` resolves with a new version. No loading state visible to user — check runs silently on mount.

### 4. Tray Icon (Rust — no new React surfaces)

**Not a React surface.** Contract for Rust tray implementation:
- Icon: static app icon when no uploads active; 8-frame animated PNG sequence at 150ms intervals when uploads active
- Frame files: `icons/tray-frame-{0..7}.png`, 22×22px each (macOS standard tray size)
- Menu text format: `{N} upload(s) — {pct}%` when active; `"No active uploads"` when idle
- Menu items in order: [status string — disabled], [separator], ["Open Face Hugger" — always enabled], ["Pause All" — enabled only when active], [separator], ["Quit"]

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| OAuth primary CTA | "Sign in with Hugging Face" |
| OAuth waiting state heading | "Waiting for browser login..." |
| OAuth waiting state body | "Complete sign-in in your browser, then return here." |
| OAuth cancel button | "Cancel Sign-in" |
| OAuth error state | "Sign-in failed — try again or use an access token instead." |
| "Use token instead" link | "Use access token instead" |
| Preview tab label | "Preview" |
| Preview unavailable heading | "Preview unavailable" |
| Preview unavailable body | "This dataset hasn't been processed for preview yet." |
| Preview loading state | (skeleton only — no text) |
| Search bar placeholder | "Search rows..." |
| Split selector label | "Split:" |
| Column stat panel title | "{ColumnName}" (exact column name, no additional label) |
| Partial statistics notice | "Statistics are partial — dataset exceeds 5 GB." |
| Pagination footer | "Rows {start}–{end} of {total}" |
| Update banner message | "Version {X.Y.Z} is available" |
| Update now button | "Update now" |
| Dismiss button | "Dismiss" |
| Tray no-uploads status | "No active uploads" |
| Tray active-uploads status | "{N} upload(s) — {pct}%" |
| Tray open item | "Open Face Hugger" |
| Tray pause item | "Pause All" |
| Tray quit item | "Quit" |
| Notification title (success) | "Upload Complete" |
| Notification body (success) | "{filename} uploaded to {repo}" |
| Notification title (failure) | "Upload Failed" |
| Notification body (failure) | "{filename} could not be uploaded to {repo}." |

No destructive actions introduced in Phase 4. The tray "Quit" action quits without a confirmation dialog (standard OS pattern — user has tray context). "Pause All" is non-destructive (resumable).

---

## Interaction Contracts

### OAuth Flow

1. User is on LoginScreen. If no token exists in keyring, screen shows OAuth-first layout.
2. Click "Sign in with Hugging Face" → button enters disabled+loading state immediately (prevent double-click). Browser opens HF OAuth page. Screen transitions to waiting state (spinner + waiting copy + "Cancel Sign-in" button).
3. Waiting state: "Cancel Sign-in" button is `variant="secondary"`, full-width. Clicking it calls `oauth_cancel` command, returns to Default state.
4. On success callback: spinner disappears, green CheckCircle flashes (same success flash as existing token auth), then transitions to app shell.
5. On failure: waiting state shows error copy in red `text-destructive` below spinner. "Try again" link resets to Default state.

### Dataset Preview Navigation

1. User navigates to a dataset repo via repo browser. If `viewer: true` from `/is-valid`, "Preview" tab renders alongside Files and History.
2. Preview tab click → DatasetPreview mounts → `useDatasetViewer` fires `/splits` then `/rows` query → skeleton displays during load.
3. Default split pre-selected (first in list). User can change via Split selector.
4. Column header click → `ColumnStatPanel` slides in from right. Another click on same header closes it. Click on different header switches panel content.
5. Search: debounced 300ms after keystroke. Switches to `/search` endpoint. Highlights matches client-side. Clear search (X button inside input) returns to `/rows` endpoint.
6. Pagination: Previous/Next buttons. "Next" disabled on last page. "Previous" disabled on first page.

### Update Banner

1. On AppShell mount, `useUpdateCheck` hook runs silently.
2. If `update.version` returned, `UpdateBanner` renders at top of main content area.
3. "Update now" → triggers download+install (shows progress in button: "Downloading...") → `relaunch()` on completion.
4. "Dismiss" → `setUpdateAvailable(null)` — banner unmounts for the session.

### Close-to-Tray Behavior

No React UI for this — it is a Rust `RunEvent::ExitRequested` handler. The only React surface related to tray is the notification that fires on upload completion/failure (native OS notification, not in-app).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | table, badge, select, input, separator, skeleton | not required |

No third-party shadcn registries declared. `recharts` is installed via npm directly, not via a shadcn registry — standard npm vetting applies.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
