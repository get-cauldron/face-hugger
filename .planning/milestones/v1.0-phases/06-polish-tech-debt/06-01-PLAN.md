---
phase: 06-polish-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/settings/SettingsPage.tsx
autonomous: true
requirements: [UPLD-04]

must_haves:
  truths:
    - "User can change the concurrent upload limit (1-5) from the Settings page"
    - "The selected limit persists across app restarts"
    - "The limit change takes effect immediately on the running upload queue"
  artifacts:
    - path: "src/routes/settings/SettingsPage.tsx"
      provides: "Concurrent upload limit slider/select in Uploads section"
      contains: "setConcurrentLimit"
  key_links:
    - from: "src/routes/settings/SettingsPage.tsx"
      to: "src/commands/upload.ts::setConcurrentLimit"
      via: "import and call on change"
      pattern: "setConcurrentLimit"
    - from: "src/routes/settings/SettingsPage.tsx"
      to: "src/lib/preferences.ts::getPreference"
      via: "load initial value on mount"
      pattern: "getPreference.*concurrent_upload_limit"
---

<objective>
Add a concurrent upload limit control to the Settings page so users can adjust how many uploads run simultaneously (1-5) at runtime.

Purpose: UPLD-04 audit gap — the `setConcurrentLimit` Rust command exists and persists to tauri-plugin-store, but there is no UI to invoke it. Users are stuck with the default of 2.
Output: Settings page with an "Uploads" section containing a labeled select/slider for concurrent limit (1-5).
</objective>

<execution_context>
@/Users/zakkeown/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zakkeown/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/commands/upload.ts:
```typescript
export async function setConcurrentLimit(limit: number): Promise<void>
```

From src/lib/preferences.ts:
```typescript
export async function getPreference<T>(key: string, defaultValue: T): Promise<T>
```

From src/bindings.ts:
```typescript
async setConcurrentLimit(limit: number) : Promise<Result<null, string>>
```

Note: The Rust command persists to tauri-plugin-store key "concurrent_upload_limit". The preferences.ts module reads from the same store. Default is 2. Range is 1-5 (clamped in Rust).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add concurrent upload limit control to SettingsPage</name>
  <files>src/routes/settings/SettingsPage.tsx</files>
  <read_first>
    - src/routes/settings/SettingsPage.tsx
    - src/commands/upload.ts (setConcurrentLimit function signature)
    - src/lib/preferences.ts (getPreference function signature)
  </read_first>
  <action>
Add an "Uploads" section to SettingsPage between the "Appearance" and "Session" sections. This section contains a labeled control for the concurrent upload limit.

Implementation:
1. Import `useState`, `useEffect` from React (already imported or add).
2. Import `setConcurrentLimit` from `../../commands/upload`.
3. Import `getPreference` from `../../lib/preferences`.
4. Add state: `const [concurrentLimit, setConcurrentLimitState] = useState(2)`.
5. Add `useEffect` on mount that calls `getPreference<number>('concurrent_upload_limit', 2).then(setConcurrentLimitState)`.
6. Add handler `handleLimitChange(newLimit: number)` that:
   - Calls `setConcurrentLimitState(newLimit)` for immediate UI update
   - Calls `setConcurrentLimit(newLimit)` (the Rust command handles persistence and queue update)
7. Render an "Uploads" section with the same card styling as other sections:
   - Section header: "Uploads" (same `text-sm font-medium text-muted-foreground uppercase tracking-wider mb-3` style)
   - Card with `bg-card border border-border rounded-xl p-5`
   - Label: "Concurrent upload limit" with description "Maximum number of files uploading simultaneously (1-5)"
   - A row of 5 buttons (1 through 5), styled like toggle buttons:
     - Active button: `bg-primary text-primary-foreground`
     - Inactive: `bg-secondary text-foreground hover:bg-secondary/80`
     - All buttons: `w-9 h-9 rounded-md text-sm font-medium transition-colors`
   - Wrap buttons in a `flex gap-2 mt-3` container

Do NOT use a range slider (accessibility and precision matter for 5 discrete values). Use numbered toggle buttons.
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/face-hugger && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "setConcurrentLimit" src/routes/settings/SettingsPage.tsx
    - grep -q "getPreference" src/routes/settings/SettingsPage.tsx
    - grep -q "concurrent_upload_limit" src/routes/settings/SettingsPage.tsx
    - grep -q "Concurrent upload limit" src/routes/settings/SettingsPage.tsx
    - grep -q "Uploads" src/routes/settings/SettingsPage.tsx
  </acceptance_criteria>
  <done>Settings page shows "Uploads" section with 5 toggle buttons (1-5) for concurrent limit. Current value loads from tauri-plugin-store on mount (default 2). Clicking a button calls setConcurrentLimit which persists to store and updates the running queue immediately.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `npx tsc --noEmit`
- Settings page renders the Uploads section with concurrent limit control
- The control imports and calls setConcurrentLimit from commands/upload.ts
- The control reads initial value from preferences.ts on mount
</verification>

<success_criteria>
- Settings page has a visible "Uploads" section with concurrent upload limit toggle (1-5)
- Changing the value calls the existing Rust setConcurrentLimit command
- Initial value loads from tauri-plugin-store via preferences.ts
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-tech-debt/06-01-SUMMARY.md`
</output>
