---
phase: 03-upload-ui-repo-management
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/routes/upload/UploadPage.tsx
  - src/routes/upload/wizard/UploadWizard.tsx
  - src/routes/upload/wizard/StepRepoPicker.tsx
  - src/routes/upload/wizard/StepFilePicker.tsx
  - src/routes/upload/wizard/StepReview.tsx
  - src/components/repos/CreateRepoSheet.tsx
  - src/components/shell/AppShell.tsx
autonomous: true
requirements:
  - UPLD-06
  - REPO-02

must_haves:
  truths:
    - "First-time user can complete upload by following the wizard with no prior HF knowledge"
    - "Wizard step 1 shows scrollable repo list with model/dataset filter tabs and Create New button"
    - "Wizard step 2 accepts files via drag zone, Browse button, and folder picker — deduplicated by path"
    - "Wizard step 3 shows summary card with repo, file count, total size, and commit message field"
    - "After Upload click, wizard closes and files appear in upload queue"
    - "User can create a new repo from wizard step 1 or from Models/Datasets page headers"
    - "Create repo form is a slide-out sheet with name, type toggle, visibility toggle, license, and description"
  artifacts:
    - path: "src/routes/upload/wizard/UploadWizard.tsx"
      provides: "3-step wizard shell with step indicator and navigation"
      min_lines: 40
    - path: "src/routes/upload/wizard/StepRepoPicker.tsx"
      provides: "Step 1 repo selection with filter tabs and Create New"
      min_lines: 40
    - path: "src/routes/upload/wizard/StepFilePicker.tsx"
      provides: "Step 2 file selection via drag, browse, folder picker"
      min_lines: 50
    - path: "src/routes/upload/wizard/StepReview.tsx"
      provides: "Step 3 review summary and commit message"
      min_lines: 30
    - path: "src/components/repos/CreateRepoSheet.tsx"
      provides: "Slide-out panel for repo creation"
      min_lines: 40
  key_links:
    - from: "src/routes/upload/wizard/StepReview.tsx"
      to: "src/commands/upload.ts"
      via: "enqueueUpload call on Upload click"
      pattern: "enqueueUpload"
    - from: "src/components/repos/CreateRepoSheet.tsx"
      to: "src/commands/repos.ts"
      via: "createRepoAction call on form submit"
      pattern: "createRepoAction"
    - from: "src/routes/upload/wizard/StepFilePicker.tsx"
      to: "@tauri-apps/plugin-dialog"
      via: "open() for file and folder picker"
      pattern: "open\\("
---

<objective>
Build the 3-step upload wizard (UPLD-06) and the create repo slide-out sheet (REPO-02). The wizard guides first-time users through repo selection, file selection, and upload. The create repo sheet is used in wizard step 1 and as a standalone action from Models/Datasets pages.

Purpose: This is the primary upload entry point for new users — the wizard must feel effortless with no HF jargon.
Output: Complete upload wizard flow, create repo sheet, UploadPage container
</objective>

<execution_context>
@/Users/zakkeown/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zakkeown/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-upload-ui-repo-management/03-CONTEXT.md
@.planning/phases/03-upload-ui-repo-management/03-RESEARCH.md
@.planning/phases/03-upload-ui-repo-management/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/stores/uploadStore.ts:
```typescript
export const useUploadStore = create<UploadState>((set) => ({
  jobs: UploadJob[],
  progressMap: Record<string, UploadProgress>,
  wizardOpen: boolean,
  wizardRepoId: string | null,
  setJobs, updateProgress, setWizardOpen, setWizardRepoId
}));
```

From src/commands/repos.ts:
```typescript
export async function createRepoAction(params: { name: string; type: 'model' | 'dataset'; isPrivate: boolean; license?: string; description?: string; }): Promise<{ repoUrl: string; id: string }>;
```

From src/commands/upload.ts:
```typescript
export async function enqueueUpload(params: { filePath: string; repoId: string; repoType: string; revision?: string; commitMessage: string; priority?: boolean; }): Promise<UploadJob>;
```

From src/queries/useRepos.ts:
```typescript
export interface RepoItem { id: string; name: string; owner: string; private: boolean; downloads: number; lastModified: string; tags: string[]; likes: number; type: 'model' | 'dataset'; }
export function useModels(): UseQueryResult<RepoItem[]>;
export function useDatasets(): UseQueryResult<RepoItem[]>;
```

From src/lib/repoUtils.ts:
```typescript
export function formatBytes(bytes: number): string;
```

From @tauri-apps/plugin-dialog:
```typescript
export function open(options?: { multiple?: boolean; directory?: boolean; filters?: Array<{name: string; extensions: string[]}>; }): Promise<string | string[] | null>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UploadPage container and 3-step wizard shell</name>
  <files>
    src/routes/upload/UploadPage.tsx,
    src/routes/upload/wizard/UploadWizard.tsx,
    src/routes/upload/wizard/StepRepoPicker.tsx,
    src/routes/upload/wizard/StepFilePicker.tsx,
    src/routes/upload/wizard/StepReview.tsx,
    src/components/shell/AppShell.tsx
  </files>
  <read_first>
    src/components/shell/AppShell.tsx,
    src/stores/uploadStore.ts,
    src/queries/useRepos.ts,
    src/commands/upload.ts,
    src/commands/repos.ts,
    src/lib/repoUtils.ts,
    src/components/repos/RepoCard.tsx,
    src/components/repos/RepoListToolbar.tsx,
    src/app.css
  </read_first>
  <action>
1. Create `src/routes/upload/UploadPage.tsx`:
   - Container with two tabs/views: "New Upload" (wizard) and "Queue" (placeholder div for Plan 03)
   - Default view: if no active uploads, show wizard. If uploads exist, show queue with a "New Upload" button
   - Initialize upload monitoring on mount: call `startUploadMonitoring` and `listUploads` to populate uploadStore
   - This is the page that renders when sidebar "Upload" section is active

2. Create `src/routes/upload/wizard/UploadWizard.tsx`:
   - 3-step wizard with step indicator (use numbered dots with connecting line — 1. Select Repo, 2. Select Files, 3. Review & Upload)
   - Wizard state: `currentStep` (1-3), `selectedRepo` (RepoItem | null), `files` (Array<{path: string, name: string, size: number}>), `commitMessage` (string)
   - Navigation: Next/Back buttons. Next is disabled until step validation passes:
     - Step 1: repo must be selected
     - Step 2: at least 1 file must be added
     - Step 3: commit message must be non-empty
   - Step indicator shows completed steps with checkmark, current step highlighted in primary color
   - Use Tailwind semantic classes: bg-card, text-foreground, border-border, etc.

3. Create `src/routes/upload/wizard/StepRepoPicker.tsx`:
   - Per user decision: full scrollable list of user's repos (NOT a dropdown)
   - Filter tabs at top: "All" | "Models" | "Datasets" — use the existing RepoListToolbar search pattern
   - Each repo shown as a clickable card/row (reuse RepoCard style but simplified — name, type badge, private badge)
   - Selected repo has a visible highlight (ring-2 ring-primary or bg-secondary)
   - "Create New" button at top right — opens CreateRepoSheet (built in Task 2)
   - After repo creation, the new repo auto-selects (use queryClient.invalidateQueries + set selectedRepo)
   - Uses `useModels()` and `useDatasets()` hooks from useRepos.ts

4. Create `src/routes/upload/wizard/StepFilePicker.tsx`:
   - Three ways to add files (per user decision):
     a. Drag zone: large dashed-border area with drop icon and "Drag files here" text. Use a visual `onDragOver` highlight but note that actual file drops use Tauri's `onDragDropEvent` from parent — this zone is visual-only for in-wizard guidance. For files dragged directly onto this zone in the wizard, register a local `onDragDropEvent` listener that adds to the file list.
     b. "Browse Files" button: calls `open({ multiple: true, directory: false })` from @tauri-apps/plugin-dialog, returns absolute paths
     c. "Browse Folder" button: calls `open({ directory: true, multiple: false })` from @tauri-apps/plugin-dialog, returns folder path. Then use `readDir` from `@tauri-apps/plugin-fs` to recursively list all files in the folder
   - File list: table showing filename, size (formatted with formatBytes), and a remove (X) button per file
   - CRITICAL: Deduplicate files by absolute path. When adding from any source, filter out paths already in the list
   - Get file sizes using `stat` from `@tauri-apps/plugin-fs` for each added file path
   - Show total file count and total size at the bottom

5. Create `src/routes/upload/wizard/StepReview.tsx`:
   - Per user decision: summary card showing target repo name (with type badge), file count, total size
   - Commit message text field (required, default empty, placeholder "Add files via Face Hugger")
   - "Upload" button at the bottom — on click:
     a. For each file in the list, call `enqueueUpload({ filePath: file.path, repoId: selectedRepo.id, repoType: selectedRepo.type, commitMessage })`
     b. Close the wizard (set wizardOpen to false or navigate to queue view)
     c. The uploaded files appear in the queue (handled by uploadStore monitoring)
   - Show a loading spinner on the Upload button while enqueuing

6. Wire UploadPage into AppShell:
   - Import UploadPage in AppShell.tsx
   - Add `case 'upload': return <UploadPage />;` in ContentArea switch
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/face-hugger && ls src/routes/upload/UploadPage.tsx src/routes/upload/wizard/UploadWizard.tsx src/routes/upload/wizard/StepRepoPicker.tsx src/routes/upload/wizard/StepFilePicker.tsx src/routes/upload/wizard/StepReview.tsx && grep -q "enqueueUpload" src/routes/upload/wizard/StepReview.tsx && grep -q "UploadPage" src/components/shell/AppShell.tsx && npm run test && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/upload/UploadPage.tsx exists and exports a component
    - src/routes/upload/wizard/UploadWizard.tsx exists with step indicator rendering
    - grep "currentStep" src/routes/upload/wizard/UploadWizard.tsx returns a match
    - src/routes/upload/wizard/StepRepoPicker.tsx exists with repo list rendering
    - grep "useModels\|useDatasets" src/routes/upload/wizard/StepRepoPicker.tsx returns a match
    - src/routes/upload/wizard/StepFilePicker.tsx exists with file picking
    - grep "open(" src/routes/upload/wizard/StepFilePicker.tsx returns a match (dialog open)
    - grep "deduplic\|filter.*path\|Set.*path\|has(" src/routes/upload/wizard/StepFilePicker.tsx returns a match (deduplication logic)
    - src/routes/upload/wizard/StepReview.tsx exists with enqueue call
    - grep "enqueueUpload" src/routes/upload/wizard/StepReview.tsx returns a match
    - grep "UploadPage" src/components/shell/AppShell.tsx returns a match
    - npm run test passes
  </acceptance_criteria>
  <done>Upload wizard with 3 steps (repo picker, file picker, review+upload) is complete and wired into AppShell</done>
</task>

<task type="auto">
  <name>Task 2: Create the CreateRepoSheet slide-out panel</name>
  <files>
    src/components/repos/CreateRepoSheet.tsx,
    src/routes/upload/wizard/StepRepoPicker.tsx,
    src/routes/models/ModelsPage.tsx,
    src/routes/datasets/DatasetsPage.tsx
  </files>
  <read_first>
    src/commands/repos.ts,
    src/routes/upload/wizard/StepRepoPicker.tsx,
    src/routes/models/ModelsPage.tsx,
    src/routes/datasets/DatasetsPage.tsx,
    src/components/ui/sheet.tsx,
    src/app.css
  </read_first>
  <action>
1. Create `src/components/repos/CreateRepoSheet.tsx`:
   - Uses shadcn Sheet component — slides in from the right (per user decision: "slide-out panel from right")
   - Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `onCreated?: (repoId: string, repoType: 'model' | 'dataset') => void`, `defaultType?: 'model' | 'dataset'`
   - Form fields (per user decision):
     a. Repo name: text input, required. Validate: no spaces, lowercase alphanumeric + hyphens, 1-96 chars
     b. Type toggle: "Model" | "Dataset" — two toggle buttons, default to `defaultType` prop or "model"
     c. Visibility toggle: "Public" | "Private" — default public (per user decision)
     d. License picker: dropdown/select with common licenses (apache-2.0, mit, cc-by-4.0, cc-by-sa-4.0, cc-by-nc-4.0, other). Optional.
     e. Description: textarea, optional
   - Per user decision: "Always starts fresh with defaults — does not remember last-used type/visibility"
   - On submit: call `createRepoAction({ name, type, isPrivate, license, description })` from src/commands/repos.ts
   - On success: call `queryClient.invalidateQueries({ queryKey: ['repos'] })` to refresh repo lists, call `onCreated?.(result.id, type)`, close sheet
   - Show loading state on submit button, error message on failure
   - User's username is prepended to display: show "username/repo-name" preview as user types

2. Wire CreateRepoSheet into StepRepoPicker.tsx:
   - Add a state `sheetOpen` in StepRepoPicker
   - "Create New" button sets `sheetOpen = true`
   - Pass `onCreated` callback that selects the newly created repo and closes the sheet

3. Add "Create Repository" button to ModelsPage.tsx header:
   - Add a button next to the page title/toolbar that opens CreateRepoSheet with `defaultType="model"`
   - Import and render CreateRepoSheet with local open state

4. Add "Create Repository" button to DatasetsPage.tsx header:
   - Same pattern as ModelsPage but with `defaultType="dataset"`
  </action>
  <verify>
    <automated>cd /Users/zakkeown/Code/face-hugger && grep -q "CreateRepoSheet" src/components/repos/CreateRepoSheet.tsx && grep -q "CreateRepoSheet" src/routes/upload/wizard/StepRepoPicker.tsx && grep -q "CreateRepoSheet" src/routes/models/ModelsPage.tsx && grep -q "CreateRepoSheet" src/routes/datasets/DatasetsPage.tsx && grep -q "createRepoAction" src/components/repos/CreateRepoSheet.tsx && npm run test && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/repos/CreateRepoSheet.tsx exists and imports Sheet from shadcn
    - grep "createRepoAction" src/components/repos/CreateRepoSheet.tsx returns a match
    - grep "invalidateQueries" src/components/repos/CreateRepoSheet.tsx returns a match
    - grep "CreateRepoSheet" src/routes/upload/wizard/StepRepoPicker.tsx returns a match
    - grep "CreateRepoSheet\|Create" src/routes/models/ModelsPage.tsx returns a match
    - grep "CreateRepoSheet\|Create" src/routes/datasets/DatasetsPage.tsx returns a match
    - npm run test passes
  </acceptance_criteria>
  <done>CreateRepoSheet slide-out panel works from wizard step 1, ModelsPage, and DatasetsPage; new repos appear immediately in lists after creation</done>
</task>

</tasks>

<verification>
- Upload wizard completes the full 3-step flow: repo selection -> file selection -> review -> enqueue
- CreateRepoSheet accessible from 3 locations: wizard, ModelsPage, DatasetsPage
- Files are deduplicated by absolute path in StepFilePicker
- Wizard closes after Upload click, files appear in queue
- All existing tests pass
</verification>

<success_criteria>
- First-time user can follow the wizard through 3 steps and start an upload
- Create repo form slides in from right, creates repo, and auto-selects it in wizard
- Step validation prevents advancing without required data
- File picker supports drag zone, browse files, and browse folder
</success_criteria>

<output>
After completion, create `.planning/phases/03-upload-ui-repo-management/03-02-SUMMARY.md`
</output>
