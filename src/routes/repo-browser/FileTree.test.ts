import { describe, it } from 'vitest';
// buildTree is exported from FileTree.tsx — stub test added now, implementation in plan 03-04
// import { buildTree } from './FileTree';

describe('buildTree', () => {
  it.todo('builds a flat list into a hierarchical tree');
  it.todo('sorts directories before files at each level');
  it.todo('handles deeply nested paths');
  it.todo('returns empty array for empty input');
});
